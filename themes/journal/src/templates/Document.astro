---
/** Document officiel : type et référence, dates, description, téléchargement en carte, annexes. */
import type { DocumentVM } from '@communeo/core';
import type { PageContext } from '@communeo/theme-contract';
import Download from '@lucide/astro/icons/download';
import Layout from '../parts/Layout.astro';

interface Props { ctx: PageContext; document: DocumentVM }
const { ctx, document: doc } = Astro.props;
const type = (label: string) => label.split(' – ')[0];
const size = (label: string) => label.split(' – ')[1] ?? '';
---
<Layout ctx={ctx} heading={false}>
  <article class="jo-doc">
    <p class="jo-kicker">{doc.type.label}{doc.reference && <> · {doc.reference}</>}</p>
    <h1>{doc.title}</h1>
    <p class="jo-doc-dates">
      {doc.sessionDate && <>Séance du {doc.sessionDate.label} · </>}publié le <time datetime={doc.date.iso}>{doc.date.label}</time>
    </p>
    {doc.description && <p class="jo-doc-description">{doc.description}</p>}
    {doc.file && (
      <a href={doc.file.href} class="jo-document-single jo-doc-file" download>
        <span class="jo-document-icon" aria-hidden="true"><Download size={22} /></span>
        <span><strong class="jo-document-name">Télécharger le document</strong><span class="jo-document-meta">{doc.file.label}</span></span>
      </a>
    )}
    {doc.attachments.length > 0 && (
      <section aria-labelledby="jo-annexes" class="jo-documents">
        <h2 id="jo-annexes" class="jo-annexes-title">Annexes</h2>
        <ul class="jo-document-list">
          {doc.attachments.map((file) => (
            <li>
              <a href={file.href} download>
                <span class="jo-document-type" aria-hidden="true">{type(file.label)}</span>
                <span class="jo-document-name">{file.name}</span>
                <span class="jo-document-meta"><span class="cn-sr-only">{type(file.label)} – </span>{size(file.label)}</span>
              </a>
            </li>
          ))}
        </ul>
      </section>
    )}
  </article>
</Layout>
