---
/** Questions fréquentes : accordéon natif (<details>), question ouverte sur fond vert clair. */
import type { BlockOf } from '@communeo/core';
import RichText from '@communeo/ui-a11y/RichText.astro';
import Minus from '@lucide/astro/icons/minus';
import Plus from '@lucide/astro/icons/plus';

interface Props { block: BlockOf<'faq'> }
const { block } = Astro.props;
---
<section class="bo-faq" aria-label={block.title ?? 'Questions fréquentes'}>
  {block.title && <h2>{block.title}</h2>}
  <div class="bo-faq-list">
    {block.items.map((item) => (
      <details id={item.id}>
        <summary>
          <span>{item.question}</span>
          <span class="bo-faq-sign" aria-hidden="true"><Plus size={20} class="bo-faq-open" /><Minus size={20} class="bo-faq-close" /></span>
        </summary>
        <div class="bo-faq-answer"><RichText doc={item.answer} class="bo-prose" /></div>
      </details>
    ))}
  </div>
</section>
