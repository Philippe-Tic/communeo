---
/**
 * Documents officiels : tableau dense, filtrable dans le navigateur (recherche, type, année).
 * Sans JavaScript, tous les documents restent affichés et le formulaire est masqué.
 */
import type { DocumentVM, KeyLabel } from '@communeo/core';
import type { PageContext } from '@communeo/theme-contract';
import Download from '@lucide/astro/icons/download';
import Layout from '../parts/Layout.astro';

interface Props { ctx: PageContext; documents: DocumentVM[]; types: KeyLabel[]; years: number[] }
const { ctx, documents, types, years } = Astro.props;
---
<Layout ctx={ctx}>
  <p class="in-lead">
    Procès-verbaux, délibérations, arrêtés et documents budgétaires publiés par la commune,
    conformément à ses obligations légales.
  </p>

  {documents.length > 0 && (
    <form class="in-filter-form" hidden data-in-doc-filters>
      <p>
        <label for="in-doc-q">Rechercher un document</label>
        <input id="in-doc-q" type="search" name="q" placeholder="Mot-clé, référence…" data-in-doc-search />
      </p>
      <p>
        <label for="in-doc-type">Type</label>
        <select id="in-doc-type" name="type" data-in-doc-type>
          <option value="">Tous les types</option>
          {types.map((type) => <option value={type.key}>{type.label}</option>)}
        </select>
      </p>
      <p>
        <label for="in-doc-year">Année</label>
        <select id="in-doc-year" name="year" data-in-doc-year>
          <option value="">Toutes</option>
          {years.map((year) => <option value={year}>{year}</option>)}
        </select>
      </p>
    </form>
  )}

  <p class="in-meta in-count" role="status" data-in-doc-count>
    {documents.length} document{documents.length > 1 ? 's' : ''} · du plus récent au plus ancien
  </p>

  {documents.length ? (
    <div class="in-table-wrapper">
      <table class="in-table">
        <caption class="cn-sr-only">Liste des documents officiels, du plus récent au plus ancien</caption>
        <thead>
          <tr>
            <th scope="col">Date</th>
            <th scope="col">Type</th>
            <th scope="col">Titre</th>
            <th scope="col">Référence</th>
            <th scope="col">Fichier</th>
          </tr>
        </thead>
        <tbody>
          {documents.map((document) => (
            <tr data-in-doc data-type={document.type.key} data-year={document.year} data-search={`${document.title} ${document.reference ?? ''} ${document.type.label}`.toLowerCase()}>
              <td data-label="Date">
                <time datetime={document.date.iso}>{document.date.label}</time>
                {document.sessionDate && <><br /><span class="in-meta">séance du {document.sessionDate.label}</span></>}
              </td>
              <td data-label="Type">{document.type.label}</td>
              <th scope="row"><a href={document.href}>{document.title}</a></th>
              <td data-label="Référence" class="in-meta">{document.reference}</td>
              <td data-label="Fichier">
                {document.file && (
                  <a href={document.file.href} download>
                    <Download size={18} aria-hidden="true" />{document.file.label}
                    <span class="cn-sr-only"> – {document.title}</span>
                  </a>
                )}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  ) : (
    <p class="in-empty">Aucun document publié pour le moment.</p>
  )}

  <p class="in-empty" hidden data-in-doc-empty>Aucun document ne correspond à votre recherche.</p>
</Layout>

<script>
  const form = document.querySelector<HTMLFormElement>('[data-in-doc-filters]');
  const rows = [...document.querySelectorAll<HTMLTableRowElement>('[data-in-doc]')];
  const count = document.querySelector<HTMLElement>('[data-in-doc-count]');
  const empty = document.querySelector<HTMLElement>('[data-in-doc-empty]');
  if (form && rows.length) {
    form.hidden = false;
    const search = form.querySelector<HTMLInputElement>('[data-in-doc-search]')!;
    const type = form.querySelector<HTMLSelectElement>('[data-in-doc-type]')!;
    const year = form.querySelector<HTMLSelectElement>('[data-in-doc-year]')!;

    const apply = () => {
      const words = search.value.trim().toLowerCase();
      let visible = 0;
      for (const row of rows) {
        const match =
          (!words || (row.dataset.search ?? '').includes(words)) &&
          (!type.value || row.dataset.type === type.value) &&
          (!year.value || row.dataset.year === year.value);
        row.hidden = !match;
        if (match) visible += 1;
      }
      if (count) count.textContent = `${visible} document${visible > 1 ? 's' : ''} · du plus récent au plus ancien`;
      if (empty) empty.hidden = visible > 0;
    };

    form.addEventListener('submit', (event) => event.preventDefault());
    form.addEventListener('input', apply);
    form.addEventListener('change', apply);
  }
</script>
