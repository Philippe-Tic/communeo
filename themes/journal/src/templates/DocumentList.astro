---
/**
 * Documents officiels : tableau dense, filtrable dans le navigateur (recherche, type, année).
 * Sans JavaScript, tous les documents restent affichés et le formulaire est masqué.
 */
import type { DocumentVM, KeyLabel } from '@communeo/core';
import type { PageContext } from '@communeo/theme-contract';
import Download from '@lucide/astro/icons/download';
import Layout from '../parts/Layout.astro';

interface Props { ctx: PageContext; documents: DocumentVM[]; types: KeyLabel[]; years: number[] }
const { ctx, documents, types, years } = Astro.props;
---
<Layout ctx={ctx}>
  <p class="jo-lead jo-list-intro">
    Procès-verbaux, délibérations, arrêtés et documents budgétaires publiés par la commune,
    conformément à ses obligations légales.
  </p>

  {documents.length > 0 && (
    <form class="jo-filter-form cn-js-only" data-jo-doc-filters>
      <p>
        <label for="jo-doc-q">Rechercher un document</label>
        <input id="jo-doc-q" type="search" name="q" placeholder="Mot-clé, référence…" data-jo-doc-search />
      </p>
      <p>
        <label for="jo-doc-type">Type</label>
        <select id="jo-doc-type" name="type" data-jo-doc-type>
          <option value="">Tous les types</option>
          {types.map((type) => <option value={type.key}>{type.label}</option>)}
        </select>
      </p>
      <p>
        <label for="jo-doc-year">Année</label>
        <select id="jo-doc-year" name="year" data-jo-doc-year>
          <option value="">Toutes</option>
          {years.map((year) => <option value={year}>{year}</option>)}
        </select>
      </p>
    </form>
  )}

  <p class="jo-count" role="status" data-jo-doc-count>
    {documents.length} document{documents.length > 1 ? 's' : ''} · du plus récent au plus ancien
  </p>

  {documents.length ? (
    <div class="jo-table-wrapper">
      <table class="jo-table">
        <caption class="cn-sr-only">Liste des documents officiels, du plus récent au plus ancien</caption>
        <thead>
          <tr>
            <th scope="col">Date</th>
            <th scope="col">Type</th>
            <th scope="col">Titre</th>
            <th scope="col">Référence</th>
            <th scope="col">Fichier</th>
          </tr>
        </thead>
        <tbody>
          {documents.map((document) => (
            <tr data-jo-doc data-type={document.type.key} data-year={document.year} data-search={`${document.title} ${document.reference ?? ''} ${document.type.label}`.toLowerCase()}>
              <td data-label="Date">
                <time datetime={document.date.iso}>{document.date.label}</time>
                {document.sessionDate && <><br /><span class="jo-muted">séance du {document.sessionDate.label}</span></>}
              </td>
              <td data-label="Type">{document.type.label}</td>
              <th scope="row"><a href={document.href}>{document.title}</a></th>
              <td data-label="Référence" class="jo-muted">{document.reference}</td>
              <td data-label="Fichier">
                {document.file && (
                  <a href={document.file.href} download>
                    <Download size={18} aria-hidden="true" />{document.file.label}
                    <span class="cn-sr-only"> – {document.title}</span>
                  </a>
                )}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  ) : (
    <p class="jo-empty-inline">Aucun document publié pour le moment.</p>
  )}

  <p class="jo-empty-inline" hidden data-jo-doc-empty role="status">Aucun document ne correspond à votre recherche.</p>
</Layout>

<script>
  const form = document.querySelector<HTMLFormElement>('[data-jo-doc-filters]');
  const rows = [...document.querySelectorAll<HTMLTableRowElement>('[data-jo-doc]')];
  const count = document.querySelector<HTMLElement>('[data-jo-doc-count]');
  const empty = document.querySelector<HTMLElement>('[data-jo-doc-empty]');
  if (form && rows.length) {
    const search = form.querySelector<HTMLInputElement>('[data-jo-doc-search]')!;
    const type = form.querySelector<HTMLSelectElement>('[data-jo-doc-type]')!;
    const year = form.querySelector<HTMLSelectElement>('[data-jo-doc-year]')!;

    const apply = () => {
      const words = search.value.trim().toLowerCase();
      let visible = 0;
      for (const row of rows) {
        const match =
          (!words || (row.dataset.search ?? '').includes(words)) &&
          (!type.value || row.dataset.type === type.value) &&
          (!year.value || row.dataset.year === year.value);
        row.hidden = !match;
        if (match) visible += 1;
      }
      if (count) count.textContent = `${visible} document${visible > 1 ? 's' : ''} · du plus récent au plus ancien`;
      if (empty) empty.hidden = visible > 0;
    };

    form.addEventListener('submit', (event) => event.preventDefault());
    form.addEventListener('input', apply);
    form.addEventListener('change', apply);
  }
</script>
