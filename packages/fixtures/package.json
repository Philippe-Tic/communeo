{
  "name": "@communeo/fixtures",
  "version": "0.0.0",
  "private": true,
  "description": "Commune de démonstration Saint-Aubin-sur-Loire, au format de l'API Strapi, pour développer et tester les thèmes sans Strapi.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./assets/*": "./assets/*"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@communeo/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
