{
  "collectionName": "components_blocks_callouts",
  "info": {
    "displayName": "Encadré",
    "icon": "information",
    "description": "Information, attention, important ou conseil"
  },
  "options": {},
  "attributes": {
    "variant": {
      "type": "enumeration",
      "enum": [
        "info",
        "warning",
        "important",
        "tip"
      ],
      "default": "info",
      "required": true
    },
    "title": {
      "type": "string",
      "maxLength": 120
    },
    "body": {
      "type": "json",
      "required": true
    }
  }
}
